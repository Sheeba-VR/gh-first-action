name: first work flow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: print greeting
      - run: "Hello World"
      - name: print goodbye
      - run: "Done - bye"
